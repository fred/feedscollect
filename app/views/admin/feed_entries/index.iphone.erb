<table id="hor-minimalist-b">
  <tr>
    <th> </th>
    <th> </th>
    <th> </th>
  </tr>

<% @feed_entries.each do |feed_entry| %>
  <tr>
    <td><%= link_to 'Show', admin_feed_entry(feed_entry) %></td>
    <td><%= link_to 'Edit', edit_admin_feed_entry_path(feed_entry) %></td>
    <td><%= link_to 'Destroy', admin_feed_entry_path(feed_entry), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<p><%= link_to 'New Feed Entry', new_admin_feed_entry_path %></p>