<table id="hor-minimalist-b">
  <tr>
    <th>Title</th>
    <th>Desc</th>
    <th>Sites</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @categories.each do |category| %>
  <tr>
    <td><%= link_to category.title.to_s, admin_category_feed_sites_url(category) %></td>
    <td><%= category.description %></td>
    <td><%= link_to(category.feed_sites.count, admin_category_feed_sites_url(category)) %></td>
    <td><%= link_to "Show", admin_category_path(category) %> </td>
    <td><%= link_to 'Edit', edit_admin_category_path(category) %></td>
    <td><%= link_to 'Del', admin_category_path(category), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Category', new_admin_category_path %>